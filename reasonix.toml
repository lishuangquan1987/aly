# Reasonix configuration.
# Resolution order: flag > ./reasonix.toml > ~/.config/reasonix/config.toml > built-in defaults.
# Secrets come from the environment via api_key_env; never put keys here.

config_version = 2   # schema marker for diagnostics; old versions may ignore it
default_model = "deepseek-flash"
# language      = "zh"   # ui/model language; empty = auto-detect from $LANG / $REASONIX_LANG

[agent]
# system_prompt = """..."""   # omit to use the built-in prompt for this version
# system_prompt_file = "prompts/system.md"   # overrides system_prompt when set
max_steps         = 0   # executor tool-call rounds; 0 = no limit
planner_max_steps = 12   # planner read-only tool-call rounds; 0 = no limit
temperature       = 0.0
auto_plan   = "off"   # off|on; off keeps plan mode manual
# auto_plan_classifier = "deepseek-flash"   # optional; only used for borderline tasks
soft_compact_ratio  = 0.5   # notice only; keeps cache-first prefix intact
compact_ratio       = 0.8   # try compacting when prompt reaches this fraction
compact_force_ratio = 0.9   # force compacting at this high-water mark
# planner_model = "mimo"   # optional: enable two-model collaboration
# subagent_model = "deepseek-pro"   # optional default for runAs=subagent skills
# subagent_models = { review = "deepseek-pro", security_review = "deepseek-pro" }   # per-skill overrides
# subagent_effort = "high"   # optional default effort for subagents
# subagent_efforts = { review = "max", task = "high" }   # per-tool/skill effort overrides
# output_style = "explanatory"   # explanatory | learning | concise | custom; empty = default

[tools]
enabled = []   # empty = all built-in tools
bash_timeout_seconds = 120   # foreground safety cap; set 0 for no tool-local cap

[codegraph]
enabled      = true   # built-in MCP server; off by default for first-run sessions
auto_install = true   # fetch the runtime when CodeGraph is enabled but missing
# path       = ""   # empty = cache, then PATH, then a bundle beside reasonix

[lsp]
enabled = true   # language server tools; servers launch lazily when used
# [lsp.servers.go]
# command = "gopls"
# args = []
# extensions = [".go"]

[skills]
# paths = ["~/my-skills", "../shared/skills"]   # extra custom skill roots
# excluded_paths = ["~/.agents/skills"]   # hide convention roots without deleting folders
# max_depth = 3   # nested scan depth; set 1 for legacy root-only discovery
# disabled_skills = ["review"]   # hide noisy or unwanted skills

[permissions]
allow = ["Edit(README.md)", "Bash(cd E:\\Project2026\\Zap && python -c \"\nwith open('README.md', 'r', encoding='utf-8') as f:\n    content = f.read()\nlines = content.split('\\n')\n# Print all headings (## and ###) to verify structure\nfor i, line in enumerate(lines):\n    if line.startswith('## '):\n        print(f'{line}')\n    elif line.startswith('### '):\n        print(f'  {line}')\nprint(f'\\nTotal lines: {len(lines)}')\nprint(f'Total chars: {len(content)}')\n\")", "Bash(python -c \"\nwith open('E:/Project2026/aly/README.md', 'r', encoding='utf-8') as f:\n    content = f.read()\nlines = content.split('\\n')\nfor i, line in enumerate(lines):\n    if line.startswith('## '):\n        print(line)\n    elif line.startswith('### '):\n        print('  ' + line)\nprint()\nprint(f'Total lines: {len(lines)}')\nprint(f'Total chars: {len(content)}')\n\")", "Bash(which ocr 2>/dev/null || echo \"OCR_NOT_INSTALLED\")", "Bash(cd E:/Project2026/Zap && git diff README.md)", "Bash(cd E:/Project2026/aly/publish/publish-cli && go build ./cmd/publish-cli 2>&1)", "Bash(cd E:/Project2026/aly/publish/publish-cli && go build ./cmd/publish-cli 2>&1 && echo \"=== CLI OK ===\" && cd E:/Project2026/aly/server && go build ./cmd 2>&1 && echo \"=== SERVER OK ===\" && cd E:/Project2026/aly/publish/publish-gui && dotnet build --nologo 2>&1 | tail -3)", "Bash(python -c \"\nwith open('E:/Project2026/aly/client/BUILD.md', 'r', encoding='utf-8') as f:\n    content = f.read()\ncontent = content.replace('clientupdator/client', 'zap/client')\nwith open('E:/Project2026/aly/client/BUILD.md', 'w', encoding='utf-8') as f:\n    f.write(content)\nprint('BUILD.md: done')\nwith open('E:/Project2026/aly/client/build.bat', 'r', encoding='utf-8') as f:\n    content = f.read()\ncontent = content.replace('clientupdator/client', 'zap/client')\nwith open('E:/Project2026/aly/client/build.bat', 'w', encoding='utf-8') as f:\n    f.write(content)\nprint('build.bat: done')\nwith open('E:/Project2026/aly/write_agents.py', 'r', encoding='utf-8') as f:\n    content = f.read()\ncontent = content.replace('src/PublishGui/', 'src/AlyPublish/')\ncontent = content.replace('LocalApplicationData)/PublishGui/', 'LocalApplicationData)/AlyPublish/')\nwith open('E:/Project2026/aly/write_agents.py', 'w', encoding='utf-8') as f:\n    f.write(content)\nprint('write_agents.py: done')\n\" 2>&1)", "Bash(ls E:/Project2026/aly/publish/publish-cli/cmd/publish-cli/ 2>&1)", "Bash(ls -la E:/Project2026/aly/publish/publish-cli/cmd/ 2>&1)", "Edit(.github/workflows/release.yml)", "Bash(ls -la .github/workflows/release.yml 2>&1)", "Bash(python -c \"\nwith open('.github/workflows/release.yml', 'r', encoding='utf-8') as f:\n    content = f.read()\ncontent = content.replace('./cmd/publish-cli', './cmd')\nwith open('.github/workflows/release.yml', 'w', encoding='utf-8') as f:\n    f.write(content)\nprint('Fixed: ./cmd/publish-cli -> ./cmd')\n\n# Also fix unused import in check_self_update.go\nimport os\nclient_dir = 'client/cmd'\nfor fn in os.listdir(client_dir):\n    if not fn.endswith('.go'):\n        continue\n    fpath = os.path.join(client_dir, fn)\n    with open(fpath, 'r', encoding='utf-8') as f:\n        content = f.read()\n    # Check if 'config' import is unused\n    # In Go 1.10, importing 'zap/client/config' but not using 'config' by name is an error\n    with open(fpath, 'w', encoding='utf-8') as f:\n        f.write(content)\nprint('Files scanned')\n\" 2>&1)", "Bash(python -c \"\nimport os\nimport re\n\n# Check each .go file in client/cmd and client/config\ndirs = ['client/cmd', 'client/config', 'client/client', 'client/util', 'client/model']\nfor d in dirs:\n    if not os.path.exists(d):\n        continue\n    for fn in sorted(os.listdir(d)):\n        if not fn.endswith('.go'):\n            continue\n        fpath = os.path.join(d, fn)\n        with open(fpath, 'r', encoding='utf-8') as f:\n            lines = f.readlines()\n        \n        # Extract import block\n        in_import = False\n        imports = []\n        for i, line in enumerate(lines):\n            stripped = line.strip()\n            if stripped == 'import (':\n                in_import = True\n                continue\n            if in_import:\n                if stripped == ')':\n                    break\n                # Get the import path\n                imp = stripped.strip('\\\"')\n                if imp:\n                    imports.append((i+1, imp, stripped))\n        \n        # Check if each import's package name appears in the file (after the import block)\n        # This is a heuristic - it works for simple cases\n        after_import = ''.join(lines[i:])  # from end of imports\n        for lineno, imp, raw in imports:\n            pkg_name = os.path.basename(imp.rstrip('/'))\n            # Special cases: aliased imports, or multi-word\n            if pkg_name in ['config', 'model', 'util', 'client', 'cmd']:\n                if f'\\\"{imp}\\\"' not in ''.join(lines) or 'config.' not in after_import and f'\\\"zap/client/config\\\"' in raw and pkg_name == 'config':\n                    print(f'{fpath}:{lineno}: import \\\"{imp}\\\" may be unused')\n        \nprint('Done.')\n\" 2>&1)", "Bash(cd E:/Project2026/AlyUpdator && go build ./cmd 2>&1 && echo \"SERVER OK\" && cd publish/publish-cli && go build ./cmd 2>&1 && echo \"CLI OK\" && cd ../publish-gui && dotnet build --nologo 2>&1 | tail -1)", "Bash(pwd && ls -d server 2>&1)", "explore", "Bash(cd /e/Project2026/zap/client && ls go.mod 2>/dev/null; cat go.mod 2>/dev/null; echo \"---\"; go env GOMOD 2>&1)", "Bash(cd E:\\Project2026\\zap && git status --short)", "Bash(cd E:\\Project2026\\zap && git log --oneline -5)", "Bash(cd /e/Project2026/aly && python -c \"\nimport os, re\n\n# Files to check for 'zap' (case-insensitive)\ntargets = []\nfor root, dirs, files in os.walk('.'):\n    # Skip .git, .vs, .reasonix, .idea, .codegraph, .trae, .kiro, .joycode, .vscode, .github (check separately), __pycache__, bin, obj\n    skip_dirs = ['.git', '.vs', '.reasonix', '.idea', '.codegraph', '.trae', '.kiro', '.joycode', '.vscode', '__pycache__', 'bin', 'obj', '.images', '.understand-anything', '.gopath']\n    dirs[:] = [d for d in dirs if d not in skip_dirs and not d.endswith('.vs')]\n    \n    for f in files:\n        # Skip binary files\n        ext = os.path.splitext(f)[1].lower()\n        if ext in ['.exe', '.dll', '.bin', '.suo', '.db', '.vsidx', '.v2']:\n            continue\n        # Skip large files\n        fpath = os.path.join(root, f)\n        try:\n            size = os.path.getsize(fpath)\n            if size > 500000:  # skip files > 500KB\n                continue\n            if size == 0:\n                continue\n        except:\n            continue\n        \n        # Read and check\n        try:\n            with open(fpath, 'r', encoding='utf-8', errors='ignore') as fh:\n                content = fh.read()\n                if re.search(r'zap', content, re.IGNORECASE):\n                    print(f'{fpath}: FOUND zap reference')\n        except:\n            pass\n\")", "Bash(cd E:/Project2026/aly/publish/publish-cli && go build ./...)", "Bash(cd \"E:/Project2026/aly/publish/publish-cli\" && go build ./... 2>&1)", "Bash(set -e; echo \"=== Building publish-cli ===\" && cd \"E:/Project2026/aly/publish/publish-cli\" && go build ./... 2>&1 && echo \"OK\" || echo \"FAIL\")"]
[sandbox]
# Confine tool blast radius. File-writers (write_file/edit_file/multi_edit)
# may only write under workspace_root (empty = current dir) + allow_write.
# bash = "enforce" (default) jails each command in an OS sandbox (macOS now;
# graceful fallback elsewhere); "off" disables it. network allows egress.
# workspace_root = ""            # default: current working directory
# allow_write = ["/tmp"]          # extra dirs writers may also modify
bash    = "enforce"
network = true

[statusline]
# A custom status line: a command whose first stdout line replaces the built-in
# data row. It receives {"model","contextUsed","contextWindow","cwd"} as JSON on stdin.
# command = "my-statusline.sh"

# External MCP servers. type: "stdio" (default, a subprocess) | "http" | "sse".
# ${VAR} / ${VAR:-default} are expanded from the environment in command/args/env/url/headers.
# [[plugins]]
# name    = "example"
# command = "reasonix-plugin-example"
# [[plugins]]                                  # a remote server over Streamable HTTP
# name    = "stripe"
# type    = "http"
# url     = "https://mcp.stripe.com"
# headers = { Authorization = "Bearer ${STRIPE_KEY}" }
