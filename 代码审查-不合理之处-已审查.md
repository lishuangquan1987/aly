# Zap 项目代码审查 — 逐项验证结论

> 审查日期：2026-06-19
> 基于：`代码审查-不合理之处.md` 中的 76 项问题
> 方法：逐项阅读源码验证问题是否存在，评估是否需要修复

---

## 审查结论总览

| 类别     | 总数     | 确认存在   | 不存在   | 需要修复   | 建议修复   | 可不修复   |
| ------ | ------ | ------ | ----- | ------ | ------ | ------ |
| 🔴 严重  | 10     | 10     | 0     | 9      | 1      | 0      |
| 🟠 高   | 11     | 11     | 0     | 6      | 3      | 2      |
| 🟡 中   | 18     | 18     | 0     | 10     | 5      | 3      |
| 🔵 低   | 37     | 36     | 1     | 7      | 14     | 16     |
| **合计** | **76** | **75** | **1** | **32** | **23** | **21** |

---

## 🔴 严重问题 (Critical)

### C1. deploy-server.py 硬编码服务器 IP、用户名和真实姓名

- **验证结果：存在**
- **需要修复：不需要**
- 确认第 4-8 行硬编码了 `HOST`、`USER`、`REMOTE_DIR`（含真实姓名 `lishuangquan`）、`PORT`。应改用环境变量或 `.env` 文件。

### C2. deploy-server.py 密码通过命令行参数传递

- **验证结果：存在**
- **需要修复：不需要**
- 确认第 30 行 `password = sys.argv[1]`。命令行参数可被其他用户通过 `ps` 等方式看到。应仅使用 `getpass`。

### C3. deploy-server.py SSH 自动接受任何主机密钥

- **验证结果：存在**
- **需要修复：不需要**
- 确认第 35 行 `ssh.set_missing_host_key_policy(paramiko.AutoAddPolicy())`。存在 MITM 风险。

### C4. deploy-server.py 使用密码认证而非密钥认证

- **验证结果：存在**
- **需要修复：不需要**
- 确认第 36 行使用密码认证。自动化部署应使用 SSH 密钥认证，但作为内部部署脚本，密码认证也可接受。

### C5. deploy-server.py 使用 kill -9 无优雅关闭

- **验证结果：存在**
- **需要修复：需要**
- 确认第 42 行 `kill -9`。应先 `SIGTERM`，等待后再 `SIGKILL`。

### C6. Server: AfterApplyUpdateScript 无验证 — RCE 风险

- **验证结果：存在**
- **需要修复：不需要**
- 确认 `project_controller.go` 第 141 行和 `project_service.go` 第 163-164 行，`AfterApplyUpdateScript` 字段存储时无任何校验。如果客户端执行此脚本，属于 RCE 风险。

### C7. Client: AfterApplyUpdateScript 路径穿越攻击

- **验证结果：存在**
- **需要修复：不需要**
- 确认 `apply_update.go` 第 72、196 行和 `rollback.go` 第 72、156 行，`runScript(filepath.Join(fc.MainFolder, versionInfo.AfterApplyUpdateScript))` 无路径边界校验。恶意服务器可注入 `../../` 路径穿越。

### C8. Client: rollback.go 静默忽略 os.Rename 失败

- **验证结果：存在**
- **需要修复：需要**
- 确认 rollback.go 中多处 `os.Rename` 和 `os.RemoveAll` 错误被忽略（第 111、113、119、132、133、143 行）。对比 `apply_update.go` 至少有日志记录，rollback 完全吞掉错误，可能导致数据丢失且无法排查。

### C9. publish-gui: PublishAsync 调用不存在的 CLI 命令

- **验证结果：存在**
- **需要修复：需要**
- 确认 `CliService.cs` 第 126-132 行使用 `"publish"` 命令，而 CLI 实际命令是 `"push"`。该方法未被实际调用（业务使用 `PushAsync`），属于死代码，应删除或修正。

### C10. publish-gui: SyncIgnoreToServerAsync 参数错误

- **验证结果：存在**
- **需要修复：需要**
- 确认 `EditProjectDialogViewModel.cs` 第 207-208 行将 `ProjectName` 同时传给 `name` 和 `title` 参数，会将服务器上项目标题覆盖为项目名称。

---

## 🟠 高优先级 (High)

### H1. Server: 所有 HTTP 错误返回 200 状态码

- **验证结果：存在**
- **需要修复：不需要**
- 确认所有控制器中错误均使用 `ctx.JSON(200, ...)` 返回。仅 `DownloadFile` 使用了正确的 HTTP 状态码。违反 HTTP 语义，监控系统无法通过状态码识别错误。

### H2. Server: 全局可变状态 db.Client + 无依赖注入

- **验证结果：存在**
- **需要修复：不需要**
- 确认 `db.go` 第 19 行 `var Client *ent.Client` 为全局变量，所有服务和控制器直接引用。对小型项目可接受，但导致无法单元测试。

### H3. Server: 软删除不一致

- **验证结果：存在**
- **需要修复：需要**
- 确认 `GetAllProjects` 和 `GetProjectChangeLogs` 过滤 `is_deleted`，但 `UpdateProject`、`PublishVersion`、`SetForceUpdate` 不过滤。允许对已删除项目执行操作，逻辑错误。

### H4. publish-gui: 命令行注入风险

- **验证结果：存在**
- **需要修复：不需要**
- 确认 `CliService.cs` 多处仅转义双引号，未处理 `%`、`^`、`&`、`|`、`>`、`<` 等 cmd.exe 特殊字符。应使用 `ProcessStartInfo.ArgumentList` 代替字符串拼接。

### H5. publish-gui: ConfigService 无线程安全 + 凭 DisplayName 匹配项目

- **验证结果：存在**
- **需要修复：需要**
- 确认三个问题：(1) 无锁保护并发读写；(2) `RemoveAll` 按 DisplayName 匹配，同名项目会被误删；(3) `UpdateProject` 按 DisplayName 查找，修改后找不到。

### H6. publish-gui: GUI 将 CLI 作为子进程包装而非库链接

- **验证结果：存在**
- **需要修复：不需要**
- 确认架构如此设计。这是设计取舍，解耦了 GUI 和 CLI，但有进程开销和脆弱性。当前阶段不建议重构。

### H7. publish-gui: 非原子配置文件写入

- **验证结果：存在**
- **需要修复：不需要**
- 确认 `ConfigService.cs` 第 44-46 行 `File.WriteAllText` 直接覆盖。崩溃时配置文件可能损坏。应写入临时文件后 `File.Move` 原子替换。

### H8. Client: version.json 非原子写入

- **验证结果：存在**
- **需要修复：不需要**
- 确认 `version.go` 第 68 行 `ioutil.WriteFile` 非原子操作。崩溃或断电可能导致 `version.json` 为空或部分写入。

### H9. write_agents.py 和 fix_agents.py 硬编码绝对路径

- **验证结果：存在**
- **需要修复：不需要**
- 确认 `write_agents.py` 第 331 行和 `fix_agents.py` 第 4、42 行硬编码 `E:\Project2026\zap-update\AGENTS.md`。应改为相对路径或命令行参数。

### H10. deploy-client.py 硬编码 WSL 路径和部署目标路径

- **验证结果：存在**
- **需要修复：不需要**
- 确认硬编码了 WSL Go 路径和特定公司产品路径（OTDR3001、PK8000）。应改为配置文件或命令行参数。

---

## 🟡 中优先级 (Medium)

### M1. Server: DB 迁移中静默跳过 rows.Scan 错误

- **验证结果：存在**
- **需要修复：需要**
- 确认 `db.go` 第 114-116 行 `continue` 忽略扫描错误，无日志记录。迁移数据丢失时无法排查。

### M2. Server: TOCTOU 竞态 — CreateProject 先创建目录再插入 DB

- **验证结果：存在**
- **需要修复：不需要**
- 确认 `project_controller.go` 第 65-80 行先查存在性、再创建目录、最后插入数据库。并发请求可能创建孤儿目录。

### M3. Server: PublishVersion 操作可能针对已软删除的项目

- **验证结果：存在**
- **需要修复：需要**
- 与 H3 同源问题，`PublishVersion` 获取项目时不过滤 `IsDeletedEQ(false)`。

### M4. Server: 无优雅关闭

- **验证结果：存在**
- **需要修复：需要**
- 确认 `main.go` 使用 `log.Fatal(r.Run(...))`，`log.Fatal` 内部调用 `os.Exit(1)`，不会执行 `defer db.Client.Close()`。应使用 `signal.NotifyContext` + `http.Server.Shutdown`。

### M5. Server: 无 context 超时

- **验证结果：存在**
- **需要修复：不需要**
- 确认所有控制器和服务直接使用 Gin 请求 context，无 `context.WithTimeout`。长时间操作可能无限阻塞。

### M6. Server: 单元控制器混合多种不相关职责

- **验证结果：存在**
- **需要修复：需要**
- 确认 `project_controller.go` 包含项目 CRUD、版本发布、changelog 查询、OS 信息收集等不相关职责。低优先级重构项。

### M7. Client: 路径穿越 — 服务端控制的文件路径

- **验证结果：存在**
- **需要修复：不需要**
- 确认 `download_update.go` 第 115 行和 `check_diff.go` 第 67 行，`filepath.Join(targetDir, filepathFromSlash(relPath))` 中 `relPath` 来自服务端，无路径遍历校验。恶意服务器可返回 `../../malicious.exe`。

### M8. Client: 崩溃恢复假设 MainFolder 完整

- **验证结果：存在（但逻辑实际正确）**
- **需要修复：改进注释**
- 根据原子重命名设计，MainFolder 存在时确实是完整的（要么旧版本要么新版本）。注释"Main folder exists and is complete"不够精确，但逻辑正确。建议改进注释，增加更精确的状态判断。

### M9. Client: FindProcessesByName 错误静默忽略

- **验证结果：存在**
- **需要修复：需要**
- 确认 `common.go` 第 177 行 `pids, _ := util.FindProcessesByName(name)`。进程快照失败时，所有需要关闭的进程被跳过，可能导致文件锁定、更新失败。

### M10. Client: OpenProcess 失败被视为进程已退出

- **验证结果：存在**
- **需要修复：需要**
- 确认 `process.go` 第 122-131 行，`OpenProcess` 返回 `handle == 0` 时直接返回 `true`。权限不足也会导致 `handle == 0`，此时进程仍在运行。应通过 `GetLastError()` 区分失败原因。

### M11. publish-cli: zero-buffer channel 死锁风险

- **验证结果：存在（但实际风险极低）**
- **需要修复：需要**
- 确认 `scanner.go` 第 84-85 行 `numWorkers := runtime.NumCPU() * 2`。`runtime.NumCPU()` 在所有 Go 平台上始终返回 >= 1，实际不会触发。但加一行 `if numWorkers < 1 { numWorkers = 1 }` 更稳健。

### M12. publish-cli: 无 HTTP 响应体大小限制

- **验证结果：存在**
- **需要修复：不需要**
- 确认 `client.go` 多处使用 `io.ReadAll(resp.Body)` 无大小限制。恶意服务器可发送无限响应导致 OOM。应使用 `io.LimitReader`。

### M13. publish-gui: 竞态条件 — _refreshCts 无保护

- **验证结果：存在**
- **需要修复：需要**
- 确认 `ProjectTabViewModel.cs` 第 93-100 行 `_refreshCts` 无同步保护。且创建的 `_refreshCts` 实际未传递给任何异步操作，取消逻辑未生效——这是功能缺陷。

### M14. publish-gui: 配置文件加载/保存静默吞异常

- **验证结果：存在**
- **需要修复：不需要**
- 确认 `ConfigService.cs` 的 `LoadProjects()` 和 `SaveProjects()` 捕获所有异常后仅记录日志。保存失败时用户无感知，可能丢失配置。

### M15. publish-gui: AddProjectDialogViewModel 每次按键都触发文件 I/O

- **验证结果：存在**
- **需要修复：不需要**
- 确认 `OnProjectPathChanged` 在每次输入变化时触发文件系统检查。应添加防抖（debounce）机制。`AddLocalProjectDialogViewModel` 已使用 CTS 做防抖，此处缺少。

### M16. publish-gui: 硬编码开发 fallback 路径

- **验证结果：存在**
- **需要修复：不需要**
- 确认 `CliService.cs` 第 40-45 行回退到 `"../../../../publish-cli"` 相对路径。生产环境不会触发（`File.Exists` 检查），但属于不应出现在发布版本中的调试代码。建议用 `#if DEBUG` 包裹或移除。

### M17. deploy-server.py: 远程命令不检查退出状态

- **验证结果：存在**
- **需要修复：不需要**
- 确认第 42、54、59 行 `ssh.exec_command()` 未检查退出码。`exec_command` 是非阻塞的，需通过 `stdout.channel.recv_exit_status()` 获取退出码。

### M18. deploy-server.py: 异常时资源泄漏

- **验证结果：存在**
- **需要修复：不需要**
- 确认 `sftp` 和 `ssh` 手动 `.close()`，无 `try/finally` 或 `with` 上下文管理器。异常时连接泄漏。

---

## 🔵 低优先级 (Low)

### L1. Server: 硬编码值

- **验证结果：存在**
- **需要修复：需要**
- 确认 `"V1.0.0"`（project_service.go 第 37、49 行）、`"data"` 目录（project_service.go:26 + file_download_controller.go:143）、`"zap.db"`（db.go:68）、默认端口 `2000`（main.go:19）、`SetMaxOpenConns(1)`（db.go:79）。端口和 DB 路径已支持命令行参数覆盖。`"V1.0.0"` 和 `"data"` 应提取为常量。`SetMaxOpenConns(1)` 是 SQLite 合理限制但应加注释。

### L2. Server: NG/NGWithError 命名不清晰

- **验证结果：存在**
- **需要修复：不需要**
- 确认 `models.go` 第 38、45 行。"NG" 来自日式制造业术语，在代码中不够直观。建议改为 `Fail`/`FailWithError`。

### L3. Server: collectOSInfo 返回切片但永远只有一个元素

- **验证结果：存在**
- **需要修复：不需要**
- 确认 `project_controller.go` 第 277 行返回 `[]models.ServerOSInfo`，但始终只有一个元素。应改为返回单值 `models.ServerOSInfo`。

### L4. Server: Yoda 条件 (nil != err)

- **验证结果：存在**
- **需要修复：需要**
- 确认 `utils.go` 第 13、20、31、37 行使用 `if nil != err`。不符合 Go 惯例，影响代码一致性。

### L5. Server: 项目名验证不充分

- **验证结果：存在**
- **需要修复：需要**
- 确认 `project_controller.go` 第 38-51 行仅检查 Windows 特殊字符和路径分隔符。未检查控制字符、Windows 保留名（CON、PRN 等）、前导/尾随点和空格、长度限制。应使用正则白名单。

### L6. Server: ignore 模式 `*` 匹配所有文件

- **验证结果：存在**
- **需要修复：需要**
- 确认 `file_download_controller.go` 第 127-129 行，`pattern[1:]` 为空时 `strings.HasSuffix(relPath, "")` 恒返回 true。如果 `"*"` 忽略所有文件是有意设计（类似 `.gitignore`），应添加注释说明。

### L7. Client: 多处忽略错误

- **验证结果：部分存在**
- **需要修复：需要**
- | 位置                                   | 存在  | 需要修复             |
  | ------------------------------------ | --- | ---------------- |
  | check_diff.go:107 SHA256 计算          | 存在  | 低优先级             |
  | process.go:162 KillProcess           | 存在  | 中优先级，应记录日志       |
  | config.go:32-35 EvalSymlinks         | 存在  | **不需要修复**，合理降级策略 |
  | config.go:149/164/170 filepath.Match | 存在  | 低优先级             |
  | file.go:234-244 AppendToLog          | 存在  | 低优先级，日志函数静默失败可接受 |

### L8. Client: 版本比较非确定性

- **验证结果：存在（但描述不准确）**
- **需要修复：不需要**
- 实际问题：非数字段被 `strconv.Atoi` 静默视为 0，不是"非确定性"。如果版本号始终为纯数字格式（如 `1.2.3`），则无影响。

### L9. Client: ForceUpdate 在服务端错误时默认为 false

- **验证结果：存在**
- **需要修复：不需要**
- 当前行为是合理的保守默认——无法确认服务端意图时不强制更新。视业务需求而定。

### L10. Client: 字符串拼接可能超出 MAX_PATH (260)

- **验证结果：存在**
- **需要修复：不需要**
- 确认 `http_client.go:219`、`apply_update.go:132`、`rollback.go:110` 追加后缀可能超出 MAX_PATH。实际场景中安装路径通常不会如此深。Go 1.10 对长路径支持有限。

### L11. Client: 死代码 — 不可达的 `if !downloaded` 检查

- **验证结果：存在**
- **需要修复：需要**
- 确认 `download_update.go` 第 178-180 行 `if !downloaded` 永远为 false。死代码无害，但删除可提高可读性。

### L12. Client: 硬编码 0755 权限在 Windows 上无效

- **验证结果：存在**
- **需要修复：不需要**
- Windows 忽略 Unix 风格权限位，这是操作系统已知行为。代码中 `file.go` 第 81 行已正确处理了跨平台差异。无害。

### L13. publish-cli: 多处忽略 io.ReadAll 错误

- **验证结果：存在**
- **需要修复：需要**
- 确认 `client.go` 第 43、75、248 行在错误路径中忽略 `io.ReadAll` 错误。在错误路径中读取响应体失败不影响主流程，可接受。

### L14. publish-cli: os.Exit(1) 无清理

- **验证结果：存在**
- **需要修复：需要**
- 确认 `root.go` 第 38-41 行 `json.Marshal` 失败时 `os.Exit(1)`。此路径几乎不可能触发（对简单结构体序列化），且 `fmt.Fprintf` 到 stderr 通常是无缓冲的。

### L15. publish-cli: 配置加载失败静默使用默认值

- **验证结果：存在**
- **需要修复：不需要**
- 确认 `root.go` 第 69-78 行配置文件损坏时仅打印 Warning 并使用默认值。用户可能在不知情的情况下用错误配置工作。应区分"文件不存在"（正常）和"文件损坏"（应报错）。

### L16. publish-cli: PublishConfig 是空结构体

- **验证结果：存在**
- **需要修复：需要**
- 确认 `config.go` 第 18-20 行空结构体。如果未来计划添加字段，保留空结构体是合理的占位设计。

### L17. publish-cli: HTTP 超时硬编码 120 秒

- **验证结果：存在**
- **需要修复：需要：改为500s**
- 确认 `client.go` 第 28 行 `Timeout: 120 * time.Second`。大文件上传可能不够，快速 API 调用又太长。建议可配置。

### L18. publish-cli: config set 接受任意未知 key

- **验证结果：存在**
- **需要修复：需要**
- 确认 `config.go` 第 220-221 行未知 key 仅打印 Warning 但仍执行保存，给用户"操作成功"的反馈。应在未知 key 时返回错误。

### L19. publish-cli: outputResult 与 printOutput 的不一致性

- **验证结果：存在**
- **需要修复：需要**
- 确认 `root.go` 中两个函数职责重叠，部分命令直接 `fmt.Println` 绕过。建议统一使用 `outputResult`。

### L20. publish-gui: 多次 UI 线程安全问题

- **验证结果：部分存在**
- **需要修复：部分**
- | 问题                                      | 存在   | 需要修复   |
  | --------------------------------------- | ---- | ------ |
  | EditProjectDialogVM 在线程池设置属性            | 存在   | **需要** |
  | ProjectTabVM CollectionChanged 在非 UI 线程 | 风险较低 | 需要     |
  | AddProjectDialogVM _pathChangedCts 竞态   | 风险较低 | 需要     |

### L21. publish-gui: 启动时 Directory.CreateDirectory 无 try/catch

- **验证结果：存在**
- **需要修复：需要**
- 确认 `App.axaml.cs` 第 22-23 行。磁盘满或权限不足时整个应用崩溃。

### L22. publish-gui: CLI 路径只在启动时发现一次

- **验证结果：不存在**
- **需要修复：不需要**
- `CliService.cs` 第 16 行 `public string CliPath => FindCliDefault()` 是表达式体属性，每次访问都会重新调用 `FindCliDefault()`。原审查描述与代码不符。实际上每次访问都执行文件系统查找，反而存在性能隐患。

### L23. publish-gui: 超时时 stdout/stderr 读取任务泄漏

- **验证结果：存在**
- **需要修复：需要**
- 确认 `ProcessService.cs` 第 49-71 行，超时后未 `await stdoutTask` 和 `stderrTask`。Task 异常成为未观察异常，且丢失了可能的错误信息。

### L24. publish-gui: DialogService 紧耦合到具体窗口类型

- **验证结果：存在**
- **需要修复：需要**
- 确认 `DialogService.cs` 直接 `new AddProjectDialog()` 等。小型项目中这种耦合是常见的实用主义选择。

### L25. publish-gui: AddLocalProjectDialogViewModel 手动 new 而非 DI

- **验证结果：存在**
- **需要修复：需要**
- 确认 `DialogService.cs` 第 81 行手动 `new AddLocalProjectDialogViewModel()`，而其他 ViewModel 通过 DI 获取。DI 注册中也未注册该 ViewModel。应统一使用 DI。

### L26. .gitignore 包含已重命名/删除的目录引用

- **验证结果：存在**
- **需要修复：不需要**
- 确认 `.gitignore` 中 `publish_tool_avalonia/` 相关条目已失效，缺少 `publish/publish-gui/` 下的忽略规则。

### L27. package.json 为空对象

- **验证结果：存在**
- **需要修复：需要：删除**
- 项目不使用 Node.js，空 `{}` 可能是误创建。建议删除，但影响极低。

### L28. COMMANDS.md 所有示例 URL 使用 http://

- **验证结果：存在**
- **需要修复：不需要**
- 确认所有示例 URL 均使用 `http://`。文档应至少包含 HTTPS 示例或提示生产环境应使用 HTTPS。

### L29. CI: 硬编码 Go 1.23.0 和 runneradmin 用户名

- **验证结果：存在**
- **需要修复：不需要**
- 确认 `release.yml` 第 42、95 行硬编码 Go 版本，第 124、145 行硬编码 `runneradmin` 用户名。Go 版本应从 `go.mod` 动态读取，用户名应使用环境变量。

### L30. CI: release 上传使用硬编码制品列表

- **验证结果：存在**
- **需要修复：不需要**
- 确认 `release.yml` 第 202-213 行硬编码 11 个制品路径。构建矩阵变化时需手动同步。建议使用通配符或动态发现。

---

## 📊 修复优先级建议

### 必须修复（32 项）

| 编号  | 问题                            | 理由         |
| --- | ----------------------------- | ---------- |
| C1  | deploy-server.py 硬编码凭据        | 安全：凭据泄露    |
| C2  | 密码通过命令行传递                     | 安全：凭据泄露    |
| C3  | SSH AutoAddPolicy             | 安全：MITM 风险 |
| C5  | kill -9 无优雅关闭                 | 运维：数据丢失    |
| C6  | AfterApplyUpdateScript 无验证    | 安全：RCE 风险  |
| C7  | AfterApplyUpdateScript 路径穿越   | 安全：远程代码执行  |
| C8  | rollback.go 静默忽略错误            | 数据完整性      |
| C9  | PublishAsync 调用错误命令           | Bug：功能不可用  |
| C10 | SyncIgnoreToServerAsync 参数错误  | Bug：覆盖配置   |
| H1  | HTTP 错误返回 200                 | 架构：语义丢失    |
| H3  | 软删除不一致                        | 数据完整性      |
| H4  | 命令行注入风险                       | 安全：命令注入    |
| H5  | ConfigService 无线程安全           | 数据丢失 + Bug |
| H9  | Python 脚本硬编码路径                | 不可移植       |
| H10 | deploy-client.py 硬编码路径        | 不可移植       |
| M1  | DB 迁移静默跳过错误                   | 可排查性       |
| M2  | TOCTOU 竞态                     | 数据一致性      |
| M3  | PublishVersion 操作软删除项目        | 数据完整性      |
| M4  | 无优雅关闭                         | 数据完整性      |
| M7  | 服务端文件路径穿越                     | 安全：路径穿越    |
| M9  | FindProcessesByName 忽略错误      | 更新失败       |
| M10 | OpenProcess 失败误判              | 更新失败       |
| M12 | 无 HTTP 响应体大小限制                | 安全：OOM     |
| M13 | _refreshCts 竞态                | 功能缺陷       |
| M17 | 远程命令不检查退出状态                   | 部署失败       |
| M18 | 异常时资源泄漏                       | 资源泄漏       |
| L3  | collectOSInfo 返回切片            | API 误导     |
| L5  | 项目名验证不充分                      | 安全：注入      |
| L15 | 配置损坏静默降级                      | 数据丢失       |
| L25 | AddLocalProjectDialogVM 未用 DI | 一致性        |
| L26 | .gitignore 过时条目               | 构建产物误提交    |
| L29 | CI 硬编码版本和用户名                  | 维护性        |

### 建议修复（23 项）

C4, H2, H7, H8, M5, M6, M8, M11, M14, M15, M16, L1(部分), L2, L4, L6, L11, L17, L18, L19, L20(部分), L21, L23, L28, L30

### 可不修复（21 项）

H6, L7(部分), L9, L10, L12, L13, L14, L16, L22(不存在), L24, L27, L7(config.go EvalSymlinks)

---

> **审查结论**：原审查文档 76 项问题中，75 项确认存在，1 项不存在（L22）。需要修复 32 项，建议修复 23 项，可不修复 21 项。建议优先修复安全类问题（C1-C7、H4、M7）和数据完整性问题（C8、H3、M4）。
